namespace Schoolera.Domain.Enums;

/// <summary>Informational published discount type (not a payment calculation engine).</summary>
public enum FeeDiscountType
{
    FixedAmount = 1,
    Percentage = 2,
}
